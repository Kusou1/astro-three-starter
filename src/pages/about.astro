---
import Base from '@/layouts/Base.astro'
---

<Base
  title="About — astro-r3f-starter"
  description="About page with its own per-route SEO meta."
>
  <section class="page">
    <h1 data-reveal>About</h1>
    <p data-reveal>
      Different route → different cube colour (pushed through the cross-island
      store bus) → but the same GL context and the same rotation. No flash, no
      reset. This page also ships its own &lt;title&gt; / OG meta, server-rendered.
    </p>
  </section>
</Base>
